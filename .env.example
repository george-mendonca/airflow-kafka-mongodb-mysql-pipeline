# =============================================================================
# Environment Variables Configuration
# =============================================================================
# 
# IMPORTANT SECURITY NOTES:
# 
# 1. NEVER commit the actual .env file with real credentials to version control
# 2. This .env.example file should only contain example values
# 3. Copy this file to .env and replace with your actual credentials
#
# PROTECTING SENSITIVE VARIABLES:
#
# Windows (PowerShell):
#   - Use Windows Credential Manager for storing secrets
#   - Or use: $env:MYSQL_PASSWORD = "your_password" (session only)
#   - For persistent: [Environment]::SetEnvironmentVariable("MYSQL_PASSWORD", "value", "User")
#
# Linux/Mac (Bash/Zsh):
#   - Recommended: Use direnv (https://direnv.net/) for automatic loading
#   - Alternative: Use pass, 1Password CLI, or AWS Secrets Manager
#   - Set in shell: export MYSQL_PASSWORD="your_password" (session only)
#   - For persistent: Add to ~/.bashrc or ~/.zshrc (ensure file permissions are 600)
#
# =============================================================================

# -----------------------------------------------------------------------------
# ENVIRONMENT CONFIGURATION
# -----------------------------------------------------------------------------
# Options: development, staging, production
ENVIRONMENT=development

# -----------------------------------------------------------------------------
# AIRFLOW CONFIGURATION
# -----------------------------------------------------------------------------
AIRFLOW_HOME=/usr/local/airflow
EXECUTOR=LocalExecutor

# -----------------------------------------------------------------------------
# DOCKER COMPOSE vs LOCAL DEVELOPMENT
# -----------------------------------------------------------------------------
# When running with Docker Compose, use Docker service names as hosts.
# When running locally, use 'localhost' or '127.0.0.1'.
#
# The values below are configured for Docker Compose (default).
# For local development, change the hosts accordingly.
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# MONGODB CONFIGURATION (Docker Compose)
# -----------------------------------------------------------------------------
# Docker service name: mongodb
MONGODB_HOST=mongodb
MONGODB_PORT=27017
MONGODB_USER=
MONGODB_PASSWORD=
MONGODB_DATABASE=mydatabase
# Full connection URI (for compatibility)
MONGODB_URI=mongodb://mongodb:27017/mydatabase

# For local development (outside Docker), use:
# MONGODB_HOST=localhost
# MONGODB_URI=mongodb://localhost:27017/mydatabase

# -----------------------------------------------------------------------------
# MYSQL CONFIGURATION (Docker Compose)
# -----------------------------------------------------------------------------
# Docker service name: mysql
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_USER=user
MYSQL_PASSWORD=password
MYSQL_DATABASE=mydatabase

# For local development (outside Docker), use:
# MYSQL_HOST=localhost

# -----------------------------------------------------------------------------
# KAFKA CONFIGURATION (Docker Compose)
# -----------------------------------------------------------------------------
# Docker service name: kafka
KAFKA_BOOTSTRAP_SERVERS=kafka:9092
KAFKA_BROKER_URL=kafka:9092
KAFKA_TOPIC=my_topic
KAFKA_TOPIC_RAW=raw_data
KAFKA_TOPIC_PROCESSED=processed_data

# For local development (outside Docker), use:
# KAFKA_BOOTSTRAP_SERVERS=localhost:9092
# KAFKA_BROKER_URL=localhost:9092

# -----------------------------------------------------------------------------
# PIPELINE CONFIGURATION
# -----------------------------------------------------------------------------
PIPELINE_BATCH_SIZE=100
MAX_RETRIES=3

# -----------------------------------------------------------------------------
# LOGGING CONFIGURATION
# -----------------------------------------------------------------------------
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# MONGODB COLLECTION CONFIGURATION
# -----------------------------------------------------------------------------
MONGODB_COLLECTION=my_collection
